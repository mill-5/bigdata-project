# 실행
    streamlit run 파일명
    python 파일명.py

# 주피터랩 설치 + 실행
    #jupyterlab
        pip install jupyterlab 
    #분석 패키지 설치
        pip install pandas numpy plotly streamlit
    # jupyterlab 실행
    jupyter lab
        # → http://localhost:8888/lab 자동 열림

--port=8889 포트 변경
--no-browser 브라우저 자동 실행 안 함
--notebook-dir=경로 시작 디렉토리 지정
종료 = 터미널에서 Ctrl+C 두 번
좌측 파일 트리에서 BigDataAnalysis/7_week/ 로 이동 → 
                                            .ipynb 더블클릭

# 경로설정
    이동 : cd 폴더명
    상위이동 : cd ..
# venv
    활성화 : .\venv\Scripts\Activate.ps1
    오류 : Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process 
            (보안정책을 잠시 풀겠다는)


# git 
    git add .
    git commit -m "어떤 작업 했는지"
    git push origin main
         안되면 -> git push origin master

    로그인 - git config --global user.email "sympathy0513@naver.com"
            git config --global user.name "misong kim"

# pip install 
# pip streamlit (윈도우 창으로 뜨는거 실행하기 위해서)


streamlit run 파일명

??

